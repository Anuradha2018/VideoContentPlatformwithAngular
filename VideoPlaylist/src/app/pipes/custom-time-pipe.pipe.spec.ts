import { CustomTimePipePipe } from './custom-time-pipe.pipe';

describe('CustomTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
